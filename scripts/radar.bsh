#!/bin/bash


# start the dump1090/radar display

sleep 2
clear
DUMP1090=$HOME/git/dump1090curses
DATA=${DUMP1090}
if [ ! -d ${DATA} ]; then
    mkdir -m 777 ${DATA}
fi
export REGDBNAME=${DATA}/planes.db

python ${DUMP1090}/src/radar.py

